name: spendly
description: "Spendly — Premium fintech app for group and personal expense management."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  google_sign_in: ^6.2.1

  # Others
  http: ^1.2.0
  image_picker: ^1.0.7

  # State management
  flutter_riverpod: ^2.5.1

  # Navigation
  go_router: ^14.2.7

  # Charts
  fl_chart: ^0.69.0

  # Fonts
  google_fonts: ^6.2.1

  # Date/time formatting
  intl: ^0.19.0

  # Unique IDs
  uuid: ^4.4.0

  # Icons & UI
  cupertino_icons: ^1.0.8

  # Animations
  flutter_animate: ^4.5.0

  # Local storage
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true